#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>

// #define MAX_NUMBERS_SIEVE 100
#define MAX_NUMBERS_SIEVE 101

int main(){
    //long number = 100;
    // unsigned long number = 991350783547;
    // //long my_sieve[MAX_NUMBERS_SIEVE];
    // unsigned long *my_sieve = (unsigned long*)malloc(sizeof(unsigned long)*MAX_NUMBERS_SIEVE);
    // // if(!my_sieve)
    //   //fprintf(stderr, "Error: Malloc is fail\n");
  long input;
  scanf("%ld", &input);
  printf("Get number : %ld\n",input);
  // int my_sieve[MAX_NUMBERS_SIEVE];
  // for(int i = 0; i < MAX_NUMBERS_SIEVE; i++){
  //   my_sieve[i] = true;
  // }

  // for(long i = 0; i < MAX_NUMBERS_SIEVE; i++){
  //     printf("%d ",my_sieve[i]);
  // }
  //free(my_sieve);
}